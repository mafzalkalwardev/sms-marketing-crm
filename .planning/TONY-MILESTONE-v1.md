# TONY AI Agent

TONY has moved to a standalone project:

**`D:\TONY AI AGENT`**

Open that folder in Cursor to continue agent development.

SignalMint can still integrate via API (`SIGNALMINT_API_URL` in TONY's `.env`).
